# 修正：菜色詳情頁底部操作列跟分頁列疊在一起

只改一個檔案：src/App.tsx

菜色詳情頁(DishDetailPage)自己有一條固定在畫面最下面的操作列(排進菜單/
刪除)，跟全域的分頁列一樣都是 position:fixed + bottom:0，兩個疊在同一個
位置，分頁列蓋住大部分，只露出一小截綠色——就是你截圖看到的狀況。

修法：讓菜色詳情頁也跟「新增/編輯食譜」表單頁一樣隱藏掉全域分頁列，
這頁自己的操作列直接取代分頁列的位置，不會再疊在一起。
